package main

/*
import "github.com/veandco/go-sdl2/sdl"

func initsdl(name string, midifick int32) *sdl.Window {

	if sdlErr := sdl.Init(sdl.INIT_EVERYTHING); sdlErr != nil {
		panic("sdl not Init sisteam")
	}
	window, windowerr := sdl.CreateWindow("chip8 + "+name,
		sdl.WINDOWPOS_UNDEFINED,
		sdl.WINDOWPOS_UNDEFINED,
		CHIP_8_WIDTH*midifick,
		CHIP_8_HEIGHT*midifick,
		sdl.WINDOW_SHOWN,
	)

	if windowerr != nil {
		panic("load window error sisteam")
	}

	return window
}
*/
